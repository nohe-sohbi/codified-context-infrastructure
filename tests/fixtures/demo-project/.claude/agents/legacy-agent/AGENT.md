---
name: legacy-agent
description: Agent kept in the legacy nested {id}/AGENT.md layout.
tools: Read
model: sonnet
triggers: [legacy]
---

Nested-layout agent — must still be discovered by the recursive scan.
