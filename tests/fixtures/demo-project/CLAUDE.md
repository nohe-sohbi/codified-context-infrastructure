@AGENTS.md

## Claude-specific
- Use the context-retrieval MCP tools first when exploring unfamiliar code (`find_relevant_context`, `suggest_agent`, `get_index_status`).
- Respond to CONTEXT DRIFT warnings per their priority (HIGH: update the doc before other work; MEDIUM: mention to the user).
